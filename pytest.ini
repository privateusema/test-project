[pytest]
testpaths = tests
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (may require running services)
    eval: LLM evaluation tests (require API keys, may be slow)
    slow: Tests that take more than 10 seconds
addopts = -v --tb=short
